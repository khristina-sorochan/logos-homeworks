import { ProdDirective } from './prod.directive';

describe('ProdDirective', () => {
  it('should create an instance', () => {
    const directive = new ProdDirective();
    expect(directive).toBeTruthy();
  });
});
